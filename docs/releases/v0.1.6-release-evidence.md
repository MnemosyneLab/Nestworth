# v0.1.6 Release Evidence

## Status

`Implementation complete; interactive smoke and public distribution closeout pending`.

This record captures the v0.1.6 evidence available on 2026-08-21. It does not claim a live Yahoo endpoint is available, a Developer ID release is signed or notarized, or that any public artifact has been published.

## Tested environment

- macOS 26.6.2 (`25G83`) on Apple Silicon `arm64`
- Rust `1.97.1`, target `aarch64-apple-darwin`
- Bun `1.3.14`
- Package, Cargo, and Tauri versions `0.1.6`
- Tauri minimum macOS version `26.0`

## Automated evidence

- `bun run check` passed: version synchronization, generated IPC drift, ESLint, TypeScript, 26 frontend test files / 160 tests, Vite production build, 512 Rust tests, formatting, and Clippy.
- `bun run version:check`, `bun run ipc:check`, and `git diff --check` passed after the closeout documentation changes.
- The committed sanitized fixtures cover provider normalization, current refresh, daily history, migration 006, future schema 007, backup/restore, dirty invalidation, historical valuation, and analytics compatibility. Default tests make no live Yahoo request.
- An opt-in live diagnostic using only public synthetic targets (`AAPL` and `USDCNY=X`) returned HTTP `429` for both requests on 2026-08-21. No live payload was persisted; this confirms the observed rate-limit boundary only and is not evidence that Yahoo is generally available.

## Isolated bootstrap smoke

A separate debug smoke bundle was built with bundle identifier `com.waltwang.nestworth.v016smoke` so its Application Support directory could not be the production directory. The app was launched from:

`src-tauri/target/aarch64-apple-darwin/debug/bundle/macos/Nestworth.app`

Its isolated database was created at:

`/Users/waltwang/Library/Application Support/com.waltwang.nestworth.v016smoke/nestworth.sqlite3`

The readback after launch showed:

- `PRAGMA integrity_check`: `ok`
- `PRAGMA foreign_key_check`: no rows
- `_sqlx_migrations`: versions 1 through 6, all successful
- `market_data_daily_coverage` exists
- no real Application Support database was used

This proves isolated application bootstrap and schema-006 creation. Interactive UI/provider-flow smoke—manual quote offline, current Instrument/FX refresh, cached history, malformed response, 429 partial result, dirty-state visibility, explicit rebuild, and provider-cache restore—was not claimed because this environment did not grant System Events assistive access for reliable GUI interaction. The deterministic Rust and frontend suites remain the automated evidence for those contracts.

## Tested arm64 artifact

The release artifact is generated under the ignored `src-tauri/target` directory and is not committed.

- Application: `src-tauri/target/aarch64-apple-darwin/release/bundle/macos/Nestworth.app`
- Disk image: `src-tauri/target/aarch64-apple-darwin/release/bundle/dmg/Nestworth_0.1.6_aarch64.dmg`
- Embedded executable: Mach-O `arm64`
- `Info.plist`: `CFBundleShortVersionString=0.1.6`, `CFBundleVersion=0.1.6`, `LSMinimumSystemVersion=26.0`
- `hdiutil verify`: valid checksum
- SHA-256: `7a09bc646ad34516f7f4796bb3d3990bcf2df0555d5182fe8d771b3738e303e`

The local artifact is not a public release artifact. `security find-identity -v -p codesigning` found zero valid identities; the app has a linker-generated ad hoc signature with no Team ID, strict `codesign --verify --deep --strict` does not pass, and `spctl` acceptance is not claimed.

## Remaining gates

- Interactive isolated UI and device VoiceOver smoke testing across the market-data controls and dialogs.
- Developer ID signing, strict signature verification, notarization, and ticket stapling under the chosen distribution policy.
- Release-owner approval, exact artifact retention, changelog/tag finalization, checksums in the release system, and any public publication.

Until these gates are completed, v0.1.6 remains an implementation-complete release candidate and is not marked `Released`.
